<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.union.control.mapper.SensitiveDataDemoMapper">
    <insert id="insert">
        INSERT INTO sensitive_data_demo.sensitive_data_demo (phone_number)
        VALUES (#{phoneNumber})
    </insert>

    <select id="query" resultType="map">
        SELECT id, phone_number AS phoneNumber,
               DATE_FORMAT(created_at, '%Y-%m-%dT%H:%i:%s') AS createdAt
        FROM sensitive_data_demo.sensitive_data_demo
        ORDER BY id DESC
    </select>

    <insert id="insertAddressBook" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_m_announce_address_book
            (name, role, email, telephone, mobile_number)
        VALUES
            (#{name}, #{role}, #{email}, #{telephone}, #{mobileNumber})
    </insert>

    <select id="queryAddressBook"
            resultType="com.union.control.mapper.SensitiveAddressBookDemo">
        SELECT id, name, role, email, telephone,
               mobile_number AS mobileNumber,
               DATE_FORMAT(created_at, '%Y-%m-%dT%H:%i:%s') AS createdAt
        FROM t_m_announce_address_book
        ORDER BY id DESC
    </select>
    <insert id="insertSaved" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sensitive_data_demo.sensitive_data_demo (phone_number) VALUES (#{phoneNumber})
    </insert>
    <update id="update">
        UPDATE sensitive_data_demo.sensitive_data_demo SET phone_number = #{phoneNumber} WHERE id = #{id}
    </update>
    <select id="queryById" resultType="map">
        SELECT id, phone_number AS phoneNumber,
               DATE_FORMAT(created_at, '%Y-%m-%dT%H:%i:%s') AS createdAt
        FROM sensitive_data_demo.sensitive_data_demo WHERE id = #{id}
    </select>
    <update id="updateAddressBook">
        UPDATE t_m_announce_address_book
        SET name = #{name}, role = #{role}, email = #{email},
            telephone = #{telephone}, mobile_number = #{mobileNumber}
        WHERE id = #{id}
    </update>
    <select id="queryAddressBookById" resultType="com.union.control.mapper.SensitiveAddressBookDemo">
        SELECT id, name, role, email, telephone, mobile_number AS mobileNumber,
               DATE_FORMAT(created_at, '%Y-%m-%dT%H:%i:%s') AS createdAt
        FROM t_m_announce_address_book WHERE id = #{id}
    </select>
</mapper>

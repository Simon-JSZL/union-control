# Activate only for the local sensitiveProxy mock profile.
# Production must provide its existing sensitiveProxy bean.
sensitive.reveal.enabled=${SENSITIVE_REVEAL_ENABLED:false}
sensitive.reveal.ttl-seconds=${SENSITIVE_REVEAL_TTL_SECONDS:300}
sensitive.address-book.plaintext-roles=${SENSITIVE_ADDRESS_BOOK_PLAINTEXT_ROLES:}
